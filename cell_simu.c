#include <stdio.h>
#include <stdlib.h>


struct KeyVal{
	char key[50];
	int value;
};

struct Dick{
	keyVal storge[100];
	int size;
	int inx;
	void (*add_key)(char)
};

void add_key(char *key[], int *value){
	
}
